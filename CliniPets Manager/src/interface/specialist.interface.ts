export interface Specialist {
  id: string;
  name: string;
  specialty: string;  // Especialidad del especialista
  email?: string;  // Correo electrónico del especialista
  phoneNumber?: string;  // Teléfono del especialista
}
